name: field.blocks.image.name
icon: image
preview: image
fields:
  image:
    label: field.blocks.image.name
    type: files
    multiple: false
    image:
      back: black
    uploads:
      template: blocks/image
  alt:
    label: field.blocks.image.alt
    type: text
    icon: title
  caption:
    label: field.blocks.image.caption
    type: writer
    icon: text
    inline: true

  issue:
    label: Titel der Ausgabe
    type: text
    icon: text
    inline: true
  link:
    label: field.blocks.image.link
    help: Link zur Ausgabe auf E-Periodica
    type: text
    icon: url
